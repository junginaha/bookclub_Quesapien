.vercel
node_modules
.env
.env.local
.env*.local
.next
.env.production
.env.local
tsconfig.tsbuildinfo
*.env\n.env.*
.claude/worktrees/

# 이 프로젝트는 npm(package-lock.json)을 쓴다. 세션 중 sandbox에서
# pnpm build로 재검증하며 생긴 pnpm 전용 파일은 커밋 대상이 아니다.
pnpm-lock.yaml
pnpm-workspace.yaml
